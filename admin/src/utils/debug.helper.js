/**
 * Helper function to debug data.
 *
 * @param {Object} data - The data to debug.
 * @returns {void} void.
 * @since 1.0.0 
 */
export const consoleLog = (data) => {
    console.log(`\n✨ DEBUG START: ↓\n`);
    console.log(data);
    console.log('✨ DEBUG END: ↑\n\n');

}