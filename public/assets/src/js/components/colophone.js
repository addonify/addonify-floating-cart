/**
 * Handle sections of cart footer.
 *
 * @param {object} blocks. [discountBlock, subTotalBlock, totalBlock]
 * @return {void} void.
 * @since 1.2.1
 */
export function setCartColophonBlocksVisibility(blocks) {

    const { colophonSection, discountBlock, subTotalBlock, totalBlock } = blocks;

    const cartColophonEle = document.querySelector(".adfy__woofc-colophon");

    if (cartColophonEle) {

        if (colophonSection) {


        }
    }
}